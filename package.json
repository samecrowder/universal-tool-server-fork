{
  "name": "universal-tool-server",
  "version": "1.0.0",
  "description": "> We recommend that users use FastMCP and langchain-mcp-adapters to expose langchain tools via MCP using Streamable HTTP as the transport. > universal-tool-server was created before Streamable HTTP transport was introduced into the MCP specification as a stopgap solution.",
  "main": "test_streamable_client.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samecrowder/universal-tool-server-fork.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/samecrowder/universal-tool-server-fork/issues"
  },
  "homepage": "https://github.com/samecrowder/universal-tool-server-fork#readme",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.3"
  }
}
